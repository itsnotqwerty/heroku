<!DOCTYPE html>
html(lang="en")
    head
        include Includes/head.pug
    body
        div(class="container-center")
            h1= title
        textarea(name="json", cols="30", rows="10", style="margin:auto;")= json
            